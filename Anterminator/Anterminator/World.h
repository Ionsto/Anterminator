#pragma once
#include <array>
#include "Chunk.h"
class World
{
	static constexpr int ChunkSize = 100;
//World extends from -worldsize to +worldsize
//	static constexpr WorldSize = Chunk::Size & ChunkSize;
//	std::array<Chunk, WorldSize* WorldSize> ChunkArray;
};

